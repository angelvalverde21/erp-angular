import { OnlyUppercaseDirective } from './only-uppercase.directive';

describe('OnlyUppercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyUppercaseDirective();
    expect(directive).toBeTruthy();
  });
});
